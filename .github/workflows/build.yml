name: Build APK
on: push: branches: - main
jobs: build: runs-on: ubuntu-latest
steps:
  - name: Checkout código
    uses: actions/checkout@v4

  - name: Usar Node.js
    uses: actions/setup-node@v4
    with:
      node-version: 18

  - name: Instalar Capacitor
    run: |
      npm install
      npm install @capacitor/core @capacitor/cli @capacitor/android

  - name: Crear carpeta www
    run: |
      mkdir www
      cp index.html www/
      cp *.js www/ || true
      cp *.png www/ || true
      cp *.json www/ || true
      cp *.mp3 www/ || true

  - name: Inicializar Capacitor
    run: |
      npx cap init Zumbale com.zumbale.app --web-dir=www || true

  - name: Agregar Android
    run: |
      npx cap add android || true

  - name: Copiar archivos web
    run: |
      npx cap copy

  - name: Build APK
    run: |
      cd android
      chmod +x gradlew
      ./gradlew assembleDebug

  - name: Subir APK
    uses: actions/upload-artifact@v4
    with:
      name: app-debug
      path: android/app/build/outputs/apk/debug/app-debug.apk# This is a basic workflow to help you get started with Actions

